import { BusinessType } from './Business';
export interface BusinessDTO {
    name: string;
    desc: string;
    type: BusinessType;
    badge: string;
    homeRoom: number;
    investment: number;
    positions: BusinessPositionDTO[];
}
export declare type UpdateBusinessDTO = Partial<Omit<BusinessDTO, 'positions'>>;
export interface BusinessPositionDTO {
    id?: number;
    order: number;
    name: string;
    maleUniform: string;
    femaleUniform: string;
    canHire: boolean;
    canFire: boolean;
    canPromote: boolean;
    canDemote: boolean;
    isManager: boolean;
    shiftWage: number;
    shiftTime: number;
}
export declare const exampleBusinessPositionDTO: BusinessPositionDTO;
export declare const exampleBusinessDTO: BusinessDTO;
