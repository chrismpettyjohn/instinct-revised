import { User } from '@instinct-prj/interface';
export declare enum BusinessType {
    NORMAL = "NORMAL",
    HOSPITAL = "HOSPITAL"
}
export interface Business {
    id: number;
    owner: User;
    name: string;
    type: BusinessType;
    desc: string;
    badge: string;
    positions: BusinessPosition[];
}
export interface BusinessPosition {
    id: number;
    businessID: number;
    name: string;
    employees: User[];
    permissions: {
        manager: boolean;
        canHire: boolean;
        canFire: boolean;
        canPromote: boolean;
        canDemote: boolean;
    };
}
export declare const exampleBusinessPosition: BusinessPosition;
export declare const exampleBusiness: Business;
