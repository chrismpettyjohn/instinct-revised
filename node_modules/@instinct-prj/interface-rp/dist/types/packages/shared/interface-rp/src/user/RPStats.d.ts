import { BusinessPosition } from '../business/Business';
export interface BusinessPositionWithBusiness extends BusinessPosition {
    businessName: string;
}
export interface UserJob {
    businessID: number;
    positionID: number;
    businessName: string;
    positionName: string;
    businessBadge: string;
    permissions: {
        manager: boolean;
        canHire: boolean;
        canFire: boolean;
        canPromote: boolean;
        canDemote: boolean;
    };
}
export declare const exampleUserJob: UserJob;
export interface UserGang {
    gangID: number;
    rankID: number;
    gangName: string;
    rankName: string;
    gangBadge: string;
    permissions: {
        canHire: boolean;
        canFire: boolean;
        canPromote: boolean;
        canDemote: boolean;
    };
}
export interface UserRPStats {
    health: {
        current: number;
        maximum: number;
    };
    energy: {
        current: number;
        maximum: number;
    };
    job?: UserJob;
    gang?: UserGang;
}
export declare const exampleUserRPStats: UserRPStats;
