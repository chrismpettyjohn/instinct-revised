import { ConfigEntity } from './config.entity';
import { Config, ConfigDTO } from '@instinct-prj/interface';
export declare function configWire(configEntity: ConfigEntity): Config;
export declare function configDTOWire(configEntity: ConfigEntity): ConfigDTO;
